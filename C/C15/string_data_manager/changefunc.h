#ifndef CHANGEFUNC_H
#define CHANGEFUNC_H

/*
	change string in the buf to input string
	if not enough bug than change place and delete the old source
	return: 0 means ok , 1 means change failed
*/
int ChangeString();
#endif